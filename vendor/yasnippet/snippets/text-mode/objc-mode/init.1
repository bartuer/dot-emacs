#name : object one
# return type should be id, for it's subclass instance could dynamic bind
# to avoid cycle init invocation, invoke superclass's designated initializer
# superclass's init would return a object not the self, assign it to self
# --
- (id) initWith${1:$(capitalize text)}:(${2:type_of_para} *)${1:para};//move me to header
- (id) initWith${1:$(capitalize text)}:($2)$1
{
  if (self = [super ${4:designated_init}])
  {
    
    //check as early as possible
    if($1 == nil) 
    {
      [self release];
      return nil;
    }

    // assign instance variable directly, do not using accessor
    $0

  }
  return self;
}