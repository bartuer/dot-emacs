#name :singleton
# --
static ${1:class} *$1SharedInstance = nil;
$0+ ($1 *) sharedInstance;//move me to header
+ ($1 *) sharedInstance
{
   if($1SharedInstance == nil)
     $1SharedInstance = [[$1 alloc] init];

   return $1SharedInstance;
}