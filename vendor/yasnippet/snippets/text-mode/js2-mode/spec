#name : jspec suite describe
# --
JSpec.describe('`(replace-regexp-in-string ".spec.js" "" (file-name-nondirectory (buffer-file-name)))`',

  function () {
    before_each(
      function () {
      });

    desc$0
    
    after_each(
      function () {
      });

  });
