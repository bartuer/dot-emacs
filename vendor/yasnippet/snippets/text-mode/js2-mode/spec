#name : JSpec describe
# --

JSpec.describe(
  '${1:suite}',

  function () {
    before_each(
      function () {
        $0  
      });

    describe
    ('${2:case}',
     function () {
       it
       ('${3:should success}',
        function () {
          expect(true).to(be, true);
        });
     });

  });
