# name : zremrangebyscore Remove all the elements with score >= min and  score <= max from the sorted set
# (link "~/local/src/redis/doc/ZremrangebyscoreCommand.txt" 1)
# --
zremrangebyscore ${1:key} ${2:min} ${3:max}