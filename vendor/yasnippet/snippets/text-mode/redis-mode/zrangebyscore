# name : zrangebyscore Return all the elements with score >= min and score  <= max (a range query) from the sorted set
# (link "~/local/src/redis/doc/ZrangebyscoreCommand.txt" 1)
# --
zrangebyscore ${1:key} ${2:min} ${3:max}