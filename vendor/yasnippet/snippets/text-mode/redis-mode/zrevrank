# name : zrevrank Return the rank (or index) or _member_ in the sorted set  at _key_ with scores being ordered from high to low
# (link "~/local/src/redis/doc/ZrevrankCommand.txt" 1)
# --
zrevrank ${1:key} ${2:member}