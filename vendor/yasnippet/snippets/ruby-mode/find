# name: find crud model controller api rails 
# (rdoc "ActiveRecord::Base::find")
# (link "~/local/src/rails/activerecord/test/cases/finder_test.rb" 394)
# (link "~/local/src/rails/activerecord/test/cases/finder" 137)
# --
find(:all :first :last$0,
     :include,
     :joins, 
     :select_f,
     :conditions,
     :having => sql,
     :limit => ${1:number},
     :offset => ${2:number},
     :order => '${3:column} DESC',
     :group => '${4:column}',
     :readonly => true
     )